# 博客设计方案

## 1. 目标

建立一个基于 Astro + Vite 的静态博客。内容来自本地 `posts/`，友链来自 `friends-graph.json`。站点部署目标是 Vercel 或 Cloudflare Pages。

站点不提供在线编辑。所有内容更新都在本地完成，通过 git 推送触发部署。

## 2. 当前仓库判断

当前仓库更接近“历史文章库”，不是标准博客项目。

- 根目录包含 `初步需求.md`、`friends-graph.json`、`posts/`。
- `posts/` 约有 989 篇 Markdown。
- Markdown frontmatter 不统一，多数文章只有 `date`，少量文章有 `title`。
- 路径层级较深，包含中文路径、Mermaid、代码块和非 Markdown 文件。
- `posts/禁止外传/**` 需要作为受保护内容处理。

因此第一版不建议强行把全部文章迁移成 Astro Content Collections。更适合先建立一层内容索引器，统一生成文章元数据、公开文章数据、受保护密文和搜索索引。

## 3. 推荐架构

采用“自建内容索引器 + Astro 静态页面”的方案。

构建流程：

```txt
posts/ + friends-graph.json
        ↓
内容索引器
        ↓
公开文章数据 / 受保护密文 / 搜索索引 / 友链数据
        ↓
Astro 页面渲染
        ↓
静态产物部署到 Vercel 或 Cloudflare Pages
```

运行时：

```txt
公开页面：直接访问静态 HTML 或静态数据
受保护页面：输入统一密码 → 解锁当前标签页会话 → 按需解密正文
```

## 4. 内容范围

默认公开：

- `posts/**`
- 仅 Markdown 文件进入文章系统
- 排除 `docx`、`pdf`、`jpg`、`py`、`lnk` 等非 Markdown 文件

默认保护：

- `posts/禁止外传/**`

受保护文章不公开真实标题，不进入公开搜索索引，不输出明文正文。

## 5. 内容模型

每篇 Markdown 转成统一元数据：

```ts
type PostMeta = {
  id: string
  title: string
  slug: string
  sourcePath: string
  routePath: string
  date: string | null
  year: string | null
  categoryPath: string[]
  isProtected: boolean
  excerpt: string | null
  wordCount: number
  headings: Heading[]
}
```

标题推导优先级：

1. frontmatter `title`
2. 文件名
3. Markdown 一级标题

对于受保护文章，公开侧统一显示：

```txt
受保护文章
```

日期推导优先级：

1. frontmatter `date`
2. 路径中的年月信息，例如 `2025.10`
3. 无法判断则标记为`null`

不建议用文件 mtime 作为核心日期来源，因为部署环境和 git checkout 可能改变它。

## 6. 路由设计

基础页面：

```txt
/                  首页
/allposts          全部文章
/search            搜索
/friends           友链
/posts/[slug]      文章详情
```

因为项目更接近纯展示分享,所以不需要太多独立页面,尽可能做一体化设计,提高现代性阅读体验
/allposts 和 /search 可以存在，首页只放入口


文章 slug 建议使用“路径归一化 + 短 hash”生成，避免中文路径、重名文件和深层目录导致链接不稳定。
受保护文章 slug 使用不含语义的 opaque slug，例如：/posts/protected-a83f19c2d4e91b07

示例：

```txt
posts/2025.10/注意力管理.md
=> /posts/2025-10-zhu-yi-li-guan-li-a3f91
```

受保护文章:
```txt
/posts/protected-<hash>
```

## 7. 加密设计

采用静态加密内容，不做服务端鉴权，不做 cookie。

### 7.1 构建时

- 公开文章生成普通内容数据或静态 HTML。
- `posts/禁止外传/**` 不输出明文正文。
- 受保护正文被加密成静态 JSON。
- 公开元数据中的真实标题替换为“受保护文章”。
- 密文文件包含 `salt`、`iv`、`ciphertext`、加密算法版本等字段。
- 受保护文章的真实标题、日期、路径分类、摘要等私有元数据也进入加密 JSON，不进入公开元数据。

### 7.2 运行时

- 用户访问受保护文章页时看到统一密码输入界面。
- 输入正确密码后，当前标签页会话解锁。
- 解锁状态使用 `sessionStorage`，不使用 cookie。
- 文章正文按需解密，不一次性解密全部保护内容。
- 不把明文正文写入 `sessionStorage`。
- sessionStorage 不保存密码和明文正文，只保存当前标签页恢复解锁所需的派生密钥材料或会话令牌

推荐算法：

- 密钥派生：`PBKDF2-SHA-256`
- 内容加密：`AES-GCM`
- 前端 API：Web Crypto API

### 7.3 安全边界

- 这个方案适合静态站点下的内容保护。
- 明文不会出现在部署产物中。
- 保护强度依赖密码强度。
- 如果密码过弱，攻击者可以离线爆破密文。

## 8. 搜索设计

第一版只索引公开文章。

搜索索引包含：

- 公开标题
- 日期
- 全文索引(默认只搜标题和日期；用户打开“全文搜索”后再懒加载公开全文索引；刷新后回到默认状态。)

受保护文章：
  - 未解锁时，不进入全部文章、热力图和搜索结果。
  - 解锁后，受保护文章进入当前标签页的全部文章索引和热力图索引。
  - 第一版受保护文章不进入搜索索引。受保护内容搜索留到后续版本。

中文搜索建议采用本地静态方案：

- 第一选择：`Pagefind`，集成方便，适合静态站点。
- 如果中文效果不理想，再改成 `MiniSearch` + 字符 n-gram 索引。

## 9. 评论设计

推荐使用 `giscus`。

原因：

- 适合静态博客。
- 使用 GitHub Discussions 存储评论。
- 不需要自建后端。
- 和 git 驱动内容更新的模式一致。
- Vercel 和 Cloudflare Pages 都能部署。

默认设计：

- 仅公开文章显示评论区。

## 10. 友链设计

`friends-graph.json` 作为唯一数据源。

`/friends` 页面展示：

- 站点名称
- 链接
- 可选状态信息，例如 `online`、`lastChecked`、`httpStatus`

如果字段缺失，UI 保持简洁，不显示空状态。

第一版不做在线检测。`status` 字段只读取已有数据。

## 11. Markdown 渲染

需要支持：

- GitHub 风格 Markdown
- 代码高亮
- Mermaid
- 中文排版
- 表格
- 图片

建议：

- 公开文章使用 Astro Markdown 管线或统一 Markdown 渲染组件。
- 受保护文章解密后走同一套 Markdown 渲染组件，保证 UI 统一。
- Mermaid 用客户端按需渲染，避免构建阶段处理复杂图表失败。

## 12. UI 设计方向

关键词：Helvetica、现代、优雅、简洁、阅读优先。

设计原则：

- 类似solarized light的配色方案,但是因为主要显示文字,对比度可以更强一些,背景默认暖色亮色,配色上暖色亮底、低饱和主体、高对比正文、少量高饱和强调色。整体颜色和谐,保持现代性同时不显得冰冷死板
- 少量留白，要有足够信息密度。
- 首页直接展示内容，不做营销式 hero。
- 字体系统优先使用 Helvetica Neue / Helvetica / Arial fallback。
- 正文行宽控制在适合中文阅读的范围。
- 移动端优先保证阅读、搜索、归档可用。

页面结构：

```txt
首页：
最近文章(五篇)
全部文章
搜索入口
友链入口

全部文章页：
按时间的热力图可视化--同时作为索引,可交互,以周为单位,对应周的多篇文章以时间流卡片方式显示,包括日期在内的元信息要在卡片上写明
受保护文章默认不显示,提供解锁按钮

文章页：
标题
日期
路径分类
正文
评论区(公开文章显示评论区；受保护文章不加载评论区)

受保护文章页：和普通文章一样,只是在解锁按钮状态变化后才进入索引区域
按需解密
解锁后复用普通文章页样式

搜索页：
输入框
结果列表
按标题、摘要、路径展示,匹配字段变色高亮
```

## 13. 性能设计

构建性能：

- 约 989 篇 Markdown，构建时生成索引和页面。
- 加密只在构建时处理受保护目录，成本可控。

访问性能：

- 公开文章走静态页面。
- 搜索索引按需加载，不阻塞首页。
- Mermaid 只在有图表的文章中加载。
- 受保护正文按需解密，避免一次性解密全部内容。
- 评论组件延迟加载。

## 14. 部署设计

第一版以 Vercel 为主要目标，同时保持 Cloudflare Pages 兼容。

要求：

- `pnpm run build` 能稳定生成静态产物。
- 不依赖 Node 服务端运行时。
- 不依赖平台私有 API。
- 密码不通过环境变量进入前端明文。
- 受保护内容加密在构建期完成。

## 15. 实施顺序建议

第一阶段：项目壳和内容索引

- 初始化 Astro + TypeScript。
- 建立内容索引器。
- 生成文章元数据。
- 跑通首页、文章页。

第二阶段：Markdown 能力

- 代码高亮。
- Mermaid。
- 中文排版。
- 基础样式。

第三阶段：加密保护

- 标记 `posts/禁止外传/**`。
- 构建期生成密文。
- 前端密码解锁。
- `sessionStorage` 标签页会话解锁。
- 受保护文章如果被无密码访问,则统一显示“受保护文章”。

第四阶段：搜索和友链

- 公开搜索索引。
- 搜索页面。
- 友链页面读取 `friends-graph.json`。

第五阶段：评论和部署

- 接入 giscus。
- 优化构建和静态资源。
- 验证 Vercel 部署。
- 保持 Cloudflare Pages 可迁移。

## 16. 待确认决策

- 是否接受 slug 使用 ASCII + hash，而不是中文路径。-是的,浏览器路径完全ascii
- 受保护文章是否完全不进搜索结果。-是的
- 受保护文章是否默认不显示评论。-完全不需要评论组件
- 是否确认第一版只保护 `posts/禁止外传/**`。-是的
- 是否接受 `giscus` 作为评论系统。-是的
- 是否优先 Vercel，Cloudflare Pages 作为兼容目标。-是的

## 17. 信息保护
- 文章内容注明个人原创，采用 CC BY-NC-ND 4.0 协议。
- 站点代码采用 LGPL-3.0-only 协议。
- 版权年份使用 2026。